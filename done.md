# Completed Tasks

- [x] Fixed WebRTC crash in Expo Go by making `react-native-webrtc` import conditional in `frontend/src/utils/webrtc.ts`.
- [x] Made 3D Models Library list (`MobileModelListScreen.tsx`) responsive via dynamic grid columns and bounded layout (`useResponsive`).
- [x] Fixed 3D Model Viewer (`ThreeDModelScreen.tsx`) controls container to not stretch excessively on desktop/tablets by applying `maxWidth` and alignment.
- [x] Pushed project to GitHub.
